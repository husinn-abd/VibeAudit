# Optional web-specific overrides.
# Root .env is loaded first; apps/web/.env can override root .env values.
# Only VITE_ variables are exposed to browser code.

VITE_API_BASE_URL=http://localhost:4317
VITE_ORGANIZATION_ID=org_default
VITE_PROJECT_ID=proj_vibeaudit_demo
VITE_DEMO_MODE=true
